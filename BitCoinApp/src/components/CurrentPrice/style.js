import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    headerPrice: {
        width: '100%',
        height: 'auto',
        marginBottom: 20,
        alignItems: 'center',
    },
    currentPrice: {
        fontSize: 32,
        color: '#f50d41',
        marginBottom: 20,
        fontWeight: 'bold',
    },
    textPrice: {
        color: '#fff',
        fontSize: 16,
        fontWeight: 'bold',
    },
});

export default styles;